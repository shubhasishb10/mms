pipeline {

    agent any

    stages {

        stage {

            sh "echo ${branch}"
        }

        stage ("Change Directory") {

            sh "cd D:\jenkins\temp\codebuild"
            sh "cd"
        }
    }
}