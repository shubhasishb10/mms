pipeline {

    agent any

    stages {

        stage("Testing phase") {

            sh "echo ${branch}"
        }

        stage ("Change Directory") {

            sh "cd D:/jenkins/temp/codebuild"
            sh "cd"
        }
    }
}